apply plugin: "java-library"

sourceSets {
    main {
        java.srcDirs = ["src"]
    }
}

dependencies {
    /* local dependencies */
    api project(":com.sap.sse.common")
    api project(":org.json.simple")
}
