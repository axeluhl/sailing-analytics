apply plugin: "java"

sourceSets {
    main {
        java.srcDirs = ["src"]
    }
}

dependencies {
    /* local dependencies */
    compile project(":java:com.sap.sse.common")
    compile project(":java:com.sap.sse.security.common")
    compile project(":java:com.sap.sse.datamining.shared")
}
