apply plugin: 'java-library'

sourceSets {
    main {
        java.srcDirs = ['src']
    }
}

dependencies {
    /* local dependencies */
    implementation project(':com.sap.sailing.domain.shared.android')
    implementation project(':com.sap.sse.shared.android')
}
