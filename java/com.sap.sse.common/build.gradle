apply plugin: "java"

sourceSets {
    main {
        java.srcDirs = ["src"]
    }
}

dependencies {
    /* local dependencies */
    compile project(":java:org.json.simple")
    compile project(":java:com.sap.sse.datamining.shared")
}
