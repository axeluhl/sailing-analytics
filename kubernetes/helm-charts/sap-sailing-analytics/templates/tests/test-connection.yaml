apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sap-sailing-analytics.fullname" . }}-test-connection"
  labels:
    {{- include "sap-sailing-analytics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sap-sailing-analytics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
