apiVersion: v1
kind: Service
metadata:
  name: {{ include "sap-sailing-analytics.fullname" . }}
  labels:
    {{- include "sap-sailing-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sap-sailing-analytics.selectorLabels" . | nindent 4 }}
